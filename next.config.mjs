/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["ui-avatars.com", "lh3.googleusercontent.com", "platform-lookaside.fbsbx.com"],
    }
};

export default nextConfig;
