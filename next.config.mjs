/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["ui-avatars.com", "lh3.googleusercontent.com", "platform-lookaside.fbsbx.com", "i.imgur.com", ""],
    },
    reactStrictMode: true,
};

export default nextConfig;
